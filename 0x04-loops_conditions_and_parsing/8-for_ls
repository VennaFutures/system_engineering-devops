#!/usr/bin/env bash
# This script lists files in the current directory and displays only the part after the first dash

for file in *; do
    # Skip hidden files
    [[ $file == .* ]] && continue
    # Extract and display the part after the first dash, or the full name if no dash
    echo "${file#*-}"
done
